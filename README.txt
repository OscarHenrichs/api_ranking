
# API RANKING

Para rodar poder usar o comando 

```bash
sudo docker-compose down -v && docker-compose up -d --build
```

ou 

```bash
sudo php -S 0.0.0.0:5000 src/index.php
```

e chamar usando

```bash
curl -X GET http://localhost:8080/ranking/?movimento_id=1
```


